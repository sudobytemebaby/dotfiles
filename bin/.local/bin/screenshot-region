grim -t ppm -g "$(slurp)" - | satty \
    --filename - \
    --early-exit \
    --copy-command "wl-copy --type image/png" \
    --action-on-enter save-to-clipboard \
    --output-filename ~/Pictures/screenshots/$(date '+%Y%m%d-%H%M%S').png
