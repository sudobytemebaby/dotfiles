# Fish entry point.
#
# Configuration is split across:
#   conf.d/        auto-loaded modules (env, path, plugins, abbreviations…)
#   functions/     auto-loaded user functions
#   completions/   auto-loaded completions
#
# This file runs after every conf.d/*.fish and is reserved for last-mile
# overrides that don't fit elsewhere. Keep it empty when possible.
